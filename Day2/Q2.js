
// map
// Given [1, 2, 3, 4], create a new array with the squares of each number. 

let arr=[1,2,3,4];
let b=arr.map(x=> x*x);
console.log(b);
// Convert an array of names ["alice", "bob", "charlie"] into uppercase.

let brr=["alice", "bob", "charlie"];
let u=brr.map(x=>x.toUpperCase());
console.log(u);
